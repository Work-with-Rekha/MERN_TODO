export type Todo = {
  todo: string;
  _id: string;
  isCompleted: boolean;
  createdAt: string;
  updatedAt: string;
};

export type User = {
  username: string;
  email: string;
  password: string;
};
